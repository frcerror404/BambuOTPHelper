version: '3.9'
services:
  bambu-otp-service:
    build: .
    container_name: bambu-otp-service
    environment:
      - PORT=3000
      - GMAIL_USER=${GMAIL_USER}
      - GMAIL_APP_PASSWORD=${GMAIL_APP_PASSWORD}
      - OTP_EXPIRATION_MINUTES=${OTP_EXPIRATION_MINUTES:-5}
      - IMAP_HOST=${IMAP_HOST:-imap.gmail.com}
      - IMAP_PORT=${IMAP_PORT:-993}
      - OTP_MQTT_URL=${OTP_MQTT_URL:-mqtts://mqtt-broker:8883}
      - OTP_MQTT_TOPIC=${OTP_MQTT_TOPIC:-bambu/otp}
      - OTP_MQTT_USERNAME=${OTP_MQTT_USERNAME}
      - OTP_MQTT_PASSWORD=${OTP_MQTT_PASSWORD}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_proxy"
      - "traefik.http.routers.bambu-otp.rule=Host(`${TRAEFIK_DOMAIN}`)"
      - "traefik.http.routers.bambu-otp.entrypoints=websecure"
      - "traefik.http.routers.bambu-otp.tls=true"
      - "traefik.http.routers.bambu-otp.tls.certresolver=letsencrypt"
      - "traefik.http.services.bambu-otp.loadbalancer.server.port=3000"
    expose:
      - "3000"
    restart: unless-stopped
    networks:
      - traefik_proxy

  mqtt-broker:
    image: eclipse-mosquitto:2
    container_name: mqtt-broker
    restart: unless-stopped
    user: "1883:1883"

    ports:
      - "1883:1883"

    environment:
      - OTP_MQTT_USERNAME=${OTP_MQTT_USERNAME}
      - OTP_MQTT_PASSWORD=${OTP_MQTT_PASSWORD}

    volumes:
      # mosquitto.conf comes from git
      - ./mosquitto/mosquitto.conf:/tmp/mosquitto.conf:ro

      # runtime state lives here
      - mosq_config:/mosquitto/config
      - mosq_data:/mosquitto/data

    command: >
      sh -euxc '
        : "$$OTP_MQTT_USERNAME"
        : "$$OTP_MQTT_PASSWORD"

        # Ensure config directory exists
        mkdir -p /mosquitto/config

        # Copy config from git into writable volume
        cp /tmp/mosquitto.conf /mosquitto/config/mosquitto.conf

        # Create password file once
        if [ ! -f /mosquitto/config/passwordfile ]; then
          mosquitto_passwd -b /mosquitto/config/passwordfile \
            "$$OTP_MQTT_USERNAME" "$$OTP_MQTT_PASSWORD"
          chmod 640 /mosquitto/config/passwordfile
        fi

        exec mosquitto -c /mosquitto/config/mosquitto.conf
      '

volumes:
  mosq_config:
  mosq_data:


networks:
  traefik_proxy:
    external: true
